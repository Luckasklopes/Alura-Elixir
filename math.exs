defmodule MeuModulo.Math do # estou definindo um "submodulo" chamado math
    def soma(parametro1, parametro2), do: parametro1 + parametro2 # caso a função só tenha uma linha de comando, é possivel passa-la assim
end # soma dos 2 parametros passados para a função